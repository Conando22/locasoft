Place the Sage logo image provided as an attachment here:

Path: assets/img/services/sage-logo.png

Recommended steps:
1. Save the attached PNG as `sage-logo.png` in this folder.
2. Confirm the file is accessible: open http://127.0.0.1:8081/assets/img/services/sage-logo.png

If you prefer, reply "embed" and I will convert the image to a data-URI and insert it inline in `service-sage.html` so you can preview immediately without adding a file.